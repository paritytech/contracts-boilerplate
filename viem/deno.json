{ 
    "tasks": {
        "build": {
            "description": "Build contracts from the contracts directory and output bytecode and abi in the codegen directory",
            "command": "deno run --env-file --allow-all tools/build.ts"
        },
        "deploy": {
            "description": "Deploy contracts to the network specified in the .env file, and update addresses in the codegen directory",
            "command": "deno run --env-file --allow-all tools/deploy.ts"
        },
        "execute": {
            "description": "Execute the contracts call defined in tools/execute",
            "command": "deno run --env-file --allow-all tools/execute.ts"
        }
    }
}
